name: Byte
tags: [ Primitive, Buildin ]
version: 1.0.0
doc:
  _Byte_ is an alias for uint8 and is equivalent to uint8 in all ways.
  It is used, by convention, to distinguish byte values from 8-bit unsigned integer values.
